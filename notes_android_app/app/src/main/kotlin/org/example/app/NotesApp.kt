package org.example.app

import android.app.Application

/**
 * PUBLIC_INTERFACE
 * NotesApp
 * Application class for initializing app-wide resources (theme/datasource).
 */
class NotesApp : Application()
